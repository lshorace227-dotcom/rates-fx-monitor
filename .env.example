# 数据源无需任何 API key。研判叙述用本地 claude CLI 无头调用（走你的 Claude Code 订阅，无需 API key）。
# 此文件仅含可选项，复制为 .env 后按需改。.env 已被 .gitignore 忽略。

# 服务端口
PORT=8787

# 研判叙述引擎：本地 claude CLI（默认 sonnet，深度好，单次约 40–90s、计入 Claude Code 用量）
# 想更快更省可设 haiku；想更深可设 opus。claude 不可用/超时时自动退回纯量化。
CLAUDE_BIN=claude
CLAUDE_MODEL=sonnet
